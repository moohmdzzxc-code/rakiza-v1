name: Apply accessories simple
on:
  push:
    branches: [main]
    paths:
      - 'scripts/patch_accessories_simple.py'
      - '.github/workflows/apply-accessories-simple.yml'
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: python scripts/patch_accessories_simple.py
      - run: |
          git config user.name "Rakiza Automation"
          git config user.email "actions@users.noreply.github.com"
          git add index.html
          git diff --cached --quiet && exit 0
          git commit -m "Add simple accessories shortage entry"
          git push origin main
