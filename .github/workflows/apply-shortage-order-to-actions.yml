name: Apply shortage order to actions
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-shortage-order-to-actions.yml'
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: python scripts/patch_shortage_order_to_actions.py
      - run: |
          git config user.name "Rakiza Automation"
          git config user.email "actions@users.noreply.github.com"
          git add index.html
          git diff --cached --quiet && exit 0
          git commit -m "Link shortage orders to actions"
          git push origin main
